# Part A — PySpark (needs JDK 11; do NOT use Spark 4.x which requires JDK 17/21)
pyspark==3.5.*
pyarrow>=15
pandas>=2
huggingface_hub

# Part B — TIGER (GPU). Install torch matching your CUDA, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch
transformers>=4.44
sentence-transformers>=3.0
scikit-learn>=1.3
numpy<2
tqdm
